# W1 — Linear tool-process fixtures

Read/run things on a real system, step by step. Adds `RUN` and `REPEAT` to W0. Requires **E1**
(a tool loop with shell/file tools).

**Oracle:** the effect on a known-state sandbox filesystem and the expected commands.
**Path made knowable:** a sandbox at a fixed initial state, so the correct effect is singular.

_Fixtures: log-classifier_

